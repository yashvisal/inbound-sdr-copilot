ENVIRONMENT=development
FRONTEND_ORIGIN=http://localhost:3000
# Max concurrent lead enrichments for /api/leads/analyze (default 5).
ENRICHMENT_MAX_CONCURRENCY=5

# Optional for the next enrichment phase.
NEWS_API_KEY=
CENSUS_API_KEY=
OPENAI_API_KEY=
